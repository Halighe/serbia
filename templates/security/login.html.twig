<!DOCTYPE html>
<html lang="ru">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="css/normalize.css">
  <link rel="stylesheet" href="css/style.css" />
  <link rel="stylesheet" href="css/modals.css" />
  <script defer src="{{ asset('js/justValidate.js') }}"></script>
  <script defer src="{{ asset('js/validationLogin.js') }}"></script>
  
</head>

<body>
  <div class="auth">
    <div class="auth__modal flex">
      <div class="auth__close"></div>
      <div class="auth__container">
        <hi class="auth__title">АВТОРИЗАЦИЯ</hi>
        <form class="auth__form" id="authform" autocomplete="off" method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="auth__input-label">
                Вы вошли как {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Выход</a>
            </div>
        {% endif %}

        <label class="auth__input-label" for="username">E-mail</label>
          <div class="auth__input-border">
            <label class="input__invisible-shield">
              <input class="auth__input input__clear" placeholder="Введите E-mail" type="text" value="{{ last_username }}" maxlength="256" name="_username" id="username" autocomplete="username" required autofocus>      
                {# name="emailauth" id="emailauth" required> #}
              <img class="input-figure input-figure-hidden input-figure-auth" src="./img/x-circle.svg" alt="кнопка" />
            </label>
          </div>
          <label class="auth__input-label">Пароль</label>
          <div class="auth__input-border">
            <label class="input__invisible-shield">
              <input class="auth__input input__pass" placeholder="Введите пароль" type="password" minlength="6"
                maxlength="64" name="_password" id="password" required>                
            </label>
            <img class="input-figure input-figure-eye input-figure-auth" src="./img/password-eye.svg" alt="кнопка" />
          </div>
          <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >
          <a href="/recovery" class="auth__miss-pass">Забыли пароль?</a>
          <span class="auth__text">Еще нет аккаунта? <a class="auth__link-reg" href="/">Зарегистрироваться</a></span>
          <button type="submit" class="auth__button">Войти</button>          
          </form>        
      </div>
    </div>
  </div>

</body>
</html>        

